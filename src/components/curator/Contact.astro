---
---

<section class="section section--surface" id="contact">
  <div class="container">
    <div class="section-head">
      <p class="eyebrow">Page 5 — Contact</p>
      <h2>Ready to harness local-first AI in your workflow?</h2>
    </div>
  </div>
</section>

<style>
  .contact-intro {
    max-width: 500px;
    margin: 0 auto;
    color: var(--ink-muted);
  }
  .contact-actions {
    text-align: center;
    margin-top: var(--space-xl);
  }
  .contact-email {
    margin-top: var(--space-lg);
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--ink);
  }
  .btn--lg {
    padding: 1rem 2rem;
    font-size: 1rem;
  }
</style>
