---
const { lang = "en" } = Astro.props;
const languages = ["en", "fr", "de"];
---

<nav class="language-switcher">
  {languages.map((l) => (
    <a href={`/${l}/`} aria-current={l === lang ? "true" : undefined}>
      {l.toUpperCase()}
    </a>
  ))}
</nav>
