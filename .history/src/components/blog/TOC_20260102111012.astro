---
import type { MarkdownHeading } from "astro";
import { generateToc } from "@/utils/generateToc";
import TOCHeading from "./TOCHeading.astro";

interface Props {
    headings: MarkdownHeading[];
}

const { headings } = Astro.props;

// 1. Get the frontmatter from the current page
const { frontmatter } = Astro.props;

// 2. Only process headings if showToc is NOT explicitly false
const shouldShowToc = frontmatter?.showToc !== false && headings.length > 0;

const filteredHeadings = headings.filter((h) => h.depth >= 2 && h.depth <= 3);
const toc = generateToc(filteredHeadings);
---

{shouldShowToc && (
    <details open class="lg:sticky lg:top-12 lg:order-2 lg:-me-32 lg:basis-64">
        <summary class="title hover:marker:text-accent cursor-pointer text-lg">Table of Contents</summary>
        <nav class="ms-4 lg:w-full">
            <ol class="mt-4">
                {toc.map((heading) => <TOCHeading heading={heading} />)}
            </ol>
        </nav>
    </details>
)}

<style>
    summary {
        list-style: none;
    }
    summary::-webkit-details-marker {
        display: none;
    }
</style>